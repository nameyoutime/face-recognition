import { DateEposhPipe } from './date-eposh.pipe';

describe('DateEposhPipe', () => {
  it('create an instance', () => {
    const pipe = new DateEposhPipe();
    expect(pipe).toBeTruthy();
  });
});
